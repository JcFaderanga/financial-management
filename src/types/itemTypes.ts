export type itemTypes={
    id?: string | number,
    owner?: string,
    title?: string,
    price?: number | string,
    category?: string,
    location?: string,
    created_at?: any,
}

export type datePropertyTypes={
  startDate: string, 
  endDate: string,
  startTime?: string,
  endTime?: string,
}